假设：a表里的某个字段的数据要从b表里查询获取
=vlookup(中间关联字段，查找的区域，查找源的那一列的列号，0)
两张表的中间关联字段必须放在第一列
第二个参数是b表里 选择从第一列到源数据列的区域
第三个参数是b表里源数据的那一列的列号
第四个参数0表示精准匹配



=VLOOKUP(A:A,'[1-5月复诊 - 查询.xls]Sheet1'!$A:$F,6,0)

替换#N/A
=IFERROR(VLOOKUP(A:A,'[1-5月复诊 - 查询.xls]Sheet1'!$A:$F,6,0),"0.00") 选中这列 ctrtl+回车


ctrtl+回车一次性计算所有公式 不用下拉拖选，但是要先选中全部需要计算的区域


合并手机号相同的行并求和消费字段：使用数据透视（把手机号字段拖到行区域，把消费字段拖到值区域，值字段设置选择求和）或使用分类汇总（分类汇总前先按手机号排序）

统计重复的行：


快速选中多行：先选中最后一行或最后一行的单元格，再按ctrl+Shift+上风向键 如果选多了，可以shift减选 


两列求和： =sum(a2+f2)